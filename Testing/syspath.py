import sys
#below will show you all the packages you have installed. first one in the list will be the current working directory
print(sys.path)

#if you are importing a package from outside location (not from current working drectiry) you need to add that path to sys.path, then only you can import it

sys.path.append(/)




import randam
import datetime
import calendar
# to get current wokiing directory. use os module

import os
os.getcwd()

# to find entire file library
os.__file
